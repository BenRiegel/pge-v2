//imports ----------------------------------------------------------------------

import tagsList from '../assets/data/Tags.js';


//exports ----------------------------------------------------------------------

export default new Promise(async resolve => {
  resolve(tagsList);
});
